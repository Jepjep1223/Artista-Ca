import React from 'react'

import PropTypes from 'prop-types'

import './features18.css'

const Features18 = (props) => {
  return (
    <div
      id={props.features18Id}
      className="features18-layout349 thq-section-padding"
    ></div>
  )
}

Features18.defaultProps = {
  features18Id: '',
}

Features18.propTypes = {
  features18Id: PropTypes.string,
}

export default Features18
